pipeline {
    agent {
        node {
            label 'built-in'
            customWorkspace '/mnt/asad'
        }
    }
	stages {
        stage('new_folder') {
            steps {
                sh 'mkdir asad'
            }
        }
		
    }
}
