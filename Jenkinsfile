pipeline {
    agent {
        node {
            label 'built-in'
            customWorkspace '/mnt/junaid'
        }
    }
	stages {
        stage('new_folder') {
            steps {
                sh 'mkdir velocity'
            }
        }
		
    }
}
